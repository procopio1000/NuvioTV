name: Apply Subtitle Translation Bridge

on:
  push:
    branches:
      - feat/embedded-subtitle-translation-bridge
    paths:
      - scripts/apply_subtitle_translation_bridge.py
      - .github/workflows/apply-subtitle-translation-bridge.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feat/embedded-subtitle-translation-bridge
          fetch-depth: 0

      - name: Apply minimal PlayerScreen patch
        run: python3 scripts/apply_subtitle_translation_bridge.py

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Prepare local properties
        env:
          LOCAL_PROPERTIES_BASE64: ${{ secrets.LOCAL_PROPERTIES_BASE64 }}
          LOCAL_DEV_PROPERTIES_BASE64: ${{ secrets.LOCAL_DEV_PROPERTIES_BASE64 }}
        run: |
          if [ -n "$LOCAL_PROPERTIES_BASE64" ]; then
            printf '%s' "$LOCAL_PROPERTIES_BASE64" | base64 --decode > local.properties
          else
            touch local.properties
          fi

          if [ -n "$LOCAL_DEV_PROPERTIES_BASE64" ]; then
            printf '%s' "$LOCAL_DEV_PROPERTIES_BASE64" | base64 --decode > local.dev.properties
          else
            touch local.dev.properties
          fi

      - name: Compile fullDebug Kotlin
        run: ./gradlew :app:compileFullDebugKotlin --stacktrace

      - name: Commit validated source patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/nuvio/tv/ui/screens/player/PlayerScreen.kt
          if git diff --cached --quiet; then
            echo "No PlayerScreen changes to commit"
          else
            git commit -m "feat(player): wire embedded subtitle translation bridge"
            git push origin HEAD:feat/embedded-subtitle-translation-bridge
          fi
